// 🚩🚩🚩🚩🚩🚩🚩🚩🚩🚩🚩 Não mude esse arquivo!!!

export function calculateActive(index: number, selectedIndex: number) {
  const start = performance.now();
  while (performance.now() - start < 5) {}

  return index === selectedIndex;
}
